# Setup
- Copy the content of sample.env into a new file called .env and set the variables

- Install the dependencies
```sh
pip install -r requirements.txt
```
- Run the app
```sh
uvicorn app.main:app
```